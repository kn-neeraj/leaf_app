# session3_24-01-2026.md

## Summary

- Investigated scroll jank with Instruments; hitches showed 20–40 ms frame swap times (dropped frames).
- Time Profiler showed heavy SwiftUI hit-testing and text resolution/measurement.
- Added a longer stress-test document for scrolling in `test_markdown_files/scroll_stress.md`.
- Added a debug FPS overlay (toggle with Cmd+Shift+F) in `Leaf/Leaf/FPSOverlay.swift`.
- Switched zoom behavior to scale typography/spacing instead of scaling the entire view for smoother scrolling.
- Added a render model and cached attributed strings to reduce repeated text resolution:
  - Render model + builder in `Leaf/Leaf/MarkdownRenderModel.swift`
  - `MarkdownView` now uses `LazyVStack` and renders cached blocks in `Leaf/Leaf/MarkdownView.swift`
  - Content rebuilds cached blocks on load/zoom/scheme in `Leaf/Leaf/ContentView.swift`
- Added performance investigation notes in `context_space/perf_investigation_24-01-2026.md`.

## Result

- User reports little to no improvement; scrolling still feels janky.
